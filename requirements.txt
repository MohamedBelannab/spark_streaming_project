# Kafka
kafka-python
confluent-kafka

# Data generation
faker
pandas

# Redis
redis

# Spark (pour les jobs batch)
pyspark==3.5.1

# Airflow (si vous voulez exécuter des DAGs localement)
apache-airflow
apache-airflow-providers-Redis

python-snappy==0.7.1